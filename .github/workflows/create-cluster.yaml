name: create cluster from template
on:
  workflow_call:
    inputs:
      project:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      type:
        type: choice
        description: Type of cluster to provision
        options: 
        - eks
        - gke
      project:
        description: 'project'
        default: "demo-1"

env:
  PROJECT: ${{ github.event.inputs.project || 'demo-1' }}

jobs:
  create-cluster:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup rctl
      uses: rafay-presales/rctl-setup@main
    - name: create-cluster-from-template
      env:
        RCTL_REST_ENDPOINT: 'console.rafay.dev'
        RCTL_API_KEY: ${{ secrets.RCTL_API_KEY }}
        RCTL_API_SECRET: ${{ secrets.RCTL_API_SECRET }}
        RCTL_PROJECT: ${{ inputs.project }}
        CLUSTER_TYPE: ${{ inputs.type }}
      run: |
        yq e -i ".metadata.project = \"$RCTL_PROJECT\"" $CLUSTER_TYPE/cluster.yaml
        yq e -i ".metadata.name = \"$RCTL_PROJECT-$CLUSTER_TYPE-1\"" $CLUSTER_TYPE/cluster.yaml
        rctl apply -f $CLUSTER_TYPE/cluster.yaml
      shell: bash
